# Optional local-development overlay for connecting to PostgreSQL from the host.
# The default stack does not publish PostgreSQL because every application service
# reaches it through Docker networking (db:5432).
services:
  db:
    ports:
      - "127.0.0.1:${POSTGRES_HOST_PORT:-5434}:5432"
